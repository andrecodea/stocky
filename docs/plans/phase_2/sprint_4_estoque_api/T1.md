# Task 1: Prototipar queries de estoque

**Estimativa:** ~30 min  
**Dev:** Backend Dev 1  
**O que é e por que existe:**  
Testar as queries no REPL/script antes de implementar o router. Erros de sintaxe Supabase Python são mais fáceis de depurar em isolamento.

**Arquivos:**
- Criar: `scratch/estoque_queries.py` (arquivo temporário, não commitar)

---

### Documentação
- [`supabase-py` select com join](https://supabase.com/docs/reference/python/select) — `select("*, estoque_atual(*)")`
- [`supabase-py` insert](https://supabase.com/docs/reference/python/insert)

---

### O que você precisa fazer

> **Passo 1 — Listar produtos com estoque atual:**

```python
# TODO: conectar supabase_client
# TODO: query: produtos com join em estoque_atual → .select("*, estoque_atual(quantidade_atual)")
# TODO: imprimir os primeiros 3 resultados
```

<details>
<summary>💡 Solução — Passo 1</summary>

```python
from app.database import supabase_client

result = supabase_client.table("produtos").select("*, estoque_atual(quantidade_atual)").execute()
for p in result.data[:3]:
    print(p)
```

</details>

---

> **Passo 2 — Inserir movimentação e verificar estoque_atual:**

```python
# TODO: inserir movimentação de entrada para produto existente
# TODO: re-query estoque_atual para verificar que quantidade mudou
```

<details>
<summary>💡 Solução — Passo 2</summary>

```python
produto_id = result.data[0]["id"]
supabase_client.table("movimentacoes").insert({
    "produto_id": produto_id,
    "tipo": "entrada",
    "quantidade": 10,
}).execute()

estoque = supabase_client.table("estoque_atual").select("*").eq("produto_id", produto_id).execute()
print("Estoque após entrada:", estoque.data)
```

</details>

---

> **Passo 3 — Query de alertas (estoque abaixo do mínimo):**

```python
# TODO: JOIN produtos + estoque_atual WHERE quantidade_atual < estoque_minimo
# TODO: imprimir produtos em alerta
```

<details>
<summary>💡 Solução — Passo 3</summary>

```python
alertas = supabase_client.rpc("produtos_em_alerta").execute()
# ou via join manual:
prods = supabase_client.table("produtos").select("id, nome, estoque_minimo, estoque_atual(quantidade_atual)").execute()
em_alerta = [p for p in prods.data if (p.get("estoque_atual") or [{}])[0].get("quantidade_atual", 0) < p["estoque_minimo"]]
print("Em alerta:", em_alerta)
```

</details>

---

### Checkboxes

- [ ] **T1.1:** `scratch/estoque_queries.py` roda sem erro e imprime produtos
- [ ] **T1.2:** Inserir movimentação altera o valor em `estoque_atual`
- [ ] **T1.3:** Query de alertas retorna produtos com estoque abaixo do mínimo

---

### Resumo
> *Preencher após concluir a task.*
