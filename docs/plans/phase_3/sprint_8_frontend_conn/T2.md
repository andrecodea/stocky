# Task 2: Estoque e alertas com dados reais ✅

**Status:** Concluído no Streamlit — React Native pós-POC  
**Dev:** Frontend Dev 1

## O que foi feito (Streamlit)

`frontend/app/pages/2_estoque.py` exibe dados reais via `listar_estoque_atual()`:
- Tabela com colunas: Produto, SKU, Qtd atual, Mínimo, Unidade, Alerta
- Highlight vermelho (`#fde8e0`) em linhas com `abaixo_minimo = True`
- Banner de alertas com nome dos produtos críticos
- Cache de 30s para evitar queries repetidas

`frontend/app/pages/1_dashboard.py` exibe:
- Total de produtos, contagem de alertas, movimentações recentes
- Query direta em `movimentacoes` com join `produtos(nome)`

---

## Para React Native (pós-POC)

> Conectar `EstoqueScreen` e `AlertasScreen` aos endpoints FastAPI:

```typescript
// EstoqueScreen — GET /produtos
useEffect(() => {
  api.get('/produtos').then(r => setProdutos(r.data)).finally(() => setLoading(false));
}, []);

// AlertasScreen — GET /estoque/alertas
useEffect(() => {
  api.get('/estoque/alertas').then(r => setAlertas(r.data)).finally(() => setLoading(false));
}, []);
```

> Auth flow via Supabase:

```typescript
// LoginScreen
const { error } = await supabase.auth.signInWithPassword({ email, password });
// Sessão persiste via AsyncStorage — interceptor axios usa JWT automaticamente
```

### Checkboxes

- [x] **T2.1:** Streamlit exibe produtos reais com alertas coloridos
- [x] **T2.2:** Dashboard KPIs refletem dados do Supabase em tempo real
- [ ] **T2.3 (RN):** `GET /produtos` retorna dados na `EstoqueScreen` com Badge de status
- [ ] **T2.4 (RN):** `GET /estoque/alertas` popula `AlertasScreen` com contagem correta
