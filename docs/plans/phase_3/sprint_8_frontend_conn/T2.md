# Task 2: Auth flow + conectar telas de estoque

**Estimativa:** ~40 min  
**Dev:** Frontend Dev 1  
**O que é e por que existe:**  
Implementar login real com Supabase e substituir dados mockados da `EstoqueScreen` e `AlertasScreen` por dados da API.

**Arquivos:**
- Criar: `mobile/src/screens/LoginScreen.tsx` (versão conectada)
- Editar: `mobile/src/screens/EstoqueScreen.tsx`
- Editar: `mobile/src/screens/AlertasScreen.tsx`
- Editar: `mobile/src/navigation/AppNavigator.tsx`

---

### O que você precisa fazer

> **Passo 1 — Login Screen com Supabase Auth:**

```typescript
// TODO: campos email e password
// TODO: botão "Entrar" → supabase.auth.signInWithPassword({ email, password })
// TODO: sucesso → navegar para AppNavigator (abas)
// TODO: erro → exibir mensagem "Email ou senha inválidos"
```

<details>
<summary>💡 Solução — Passo 1</summary>

```typescript
// mobile/src/screens/LoginScreen.tsx
import React, { useState } from 'react';
import { View, TextInput, Text, TouchableOpacity, StyleSheet, Alert } from 'react-native';
import { supabase } from '../lib/supabase';
import { theme } from '../theme';

export const LoginScreen = ({ navigation }: any) => {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [loading, setLoading] = useState(false);

  const handleLogin = async () => {
    setLoading(true);
    const { error } = await supabase.auth.signInWithPassword({ email, password });
    setLoading(false);
    if (error) Alert.alert('Erro', 'Email ou senha inválidos');
  };

  return (
    <View style={styles.container}>
      <Text style={styles.title}>Stocky</Text>
      <TextInput style={styles.input} placeholder="Email" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" />
      <TextInput style={styles.input} placeholder="Senha" value={password} onChangeText={setPassword} secureTextEntry />
      <TouchableOpacity style={styles.button} onPress={handleLogin} disabled={loading}>
        <Text style={styles.buttonText}>{loading ? 'Entrando...' : 'Entrar'}</Text>
      </TouchableOpacity>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', padding: theme.spacing.xl, backgroundColor: theme.colors.background },
  title: { fontSize: 32, fontWeight: '800', color: theme.colors.primary, textAlign: 'center', marginBottom: theme.spacing.xl },
  input: { backgroundColor: theme.colors.surface, borderRadius: theme.borderRadius.md, padding: theme.spacing.md, marginBottom: theme.spacing.sm, fontSize: theme.fontSize.md, borderWidth: 1, borderColor: theme.colors.border },
  button: { backgroundColor: theme.colors.primary, borderRadius: theme.borderRadius.md, padding: theme.spacing.md, alignItems: 'center', marginTop: theme.spacing.sm },
  buttonText: { color: '#fff', fontSize: theme.fontSize.lg, fontWeight: '700' },
});
```

</details>

---

> **Passo 2 — Conectar `EstoqueScreen` à API:**

```typescript
// TODO: substituir MOCK_PRODUTOS por useState + useEffect
// TODO: useEffect → api.get('/produtos') → setProducts(response.data)
// TODO: mostrar ActivityIndicator enquanto carrega
// TODO: tratar erro com Alert
```

<details>
<summary>💡 Solução — Passo 2</summary>

```typescript
// Substituir no EstoqueScreen:
import React, { useState, useEffect } from 'react';
import { ActivityIndicator } from 'react-native';
import { api } from '../lib/api';

// Dentro do componente:
const [produtos, setProdutos] = useState<any[]>([]);
const [loading, setLoading] = useState(true);

useEffect(() => {
  api.get('/produtos').then(r => setProdutos(r.data)).finally(() => setLoading(false));
}, []);

if (loading) return <ActivityIndicator style={{ flex: 1 }} color={theme.colors.primary} />;
// usar produtos no lugar de MOCK_PRODUTOS
```

</details>

---

> **Passo 3 — Conectar `AlertasScreen`:**

```typescript
// TODO: api.get('/estoque/alertas') no useEffect → setAlertas(response.data)
```

<details>
<summary>💡 Solução — Passo 3</summary>

```typescript
useEffect(() => {
  api.get('/estoque/alertas').then(r => setAlertas(r.data)).finally(() => setLoading(false));
}, []);
```

</details>

---

### Checkboxes

- [ ] **T2.1:** Login com credenciais válidas navega para tela de Estoque
- [ ] **T2.2:** Login com credenciais inválidas exibe alerta de erro
- [ ] **T2.3:** `EstoqueScreen` exibe produtos reais do banco (não dados mockados)
- [ ] **T2.4:** `AlertasScreen` exibe alertas reais — badge conta correta

---

### Resumo
> *Preencher após concluir a task.*
