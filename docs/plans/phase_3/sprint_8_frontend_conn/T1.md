# Task 1: Supabase Auth + API client

**Estimativa:** ~35 min  
**Dev:** Frontend Dev 1  
**O que é e por que existe:**  
Configurar o cliente Supabase JS para Auth e criar um cliente HTTP axios/fetch com interceptor de JWT. Sem isso, nenhuma tela consegue chamar endpoints protegidos.

**Arquivos:**
- Criar: `mobile/src/lib/supabase.ts`
- Criar: `mobile/src/lib/api.ts`
- Criar: `mobile/src/store/authStore.ts`

---

### Documentação
- [`@supabase/supabase-js` RN](https://supabase.com/docs/reference/javascript/initializing?example=react-native-options-async-storage) — configuração para React Native com AsyncStorage
- [Axios interceptors](https://axios-http.com/docs/interceptors) — adicionar JWT automaticamente

---

### O que você precisa fazer

> **Passo 1 — Instalar dependências:**

```bash
# TODO: npx expo install @supabase/supabase-js @react-native-async-storage/async-storage
# TODO: npm install axios
# TODO: adicionar EXPO_PUBLIC_SUPABASE_URL e EXPO_PUBLIC_SUPABASE_ANON_KEY ao .env
```

<details>
<summary>💡 Solução — Passo 1</summary>

```bash
npx expo install @supabase/supabase-js @react-native-async-storage/async-storage
npm install axios
```

```env
# mobile/.env
EXPO_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJ...
EXPO_PUBLIC_API_URL=http://10.0.2.2:8000
```

</details>

---

> **Passo 2 — Configurar cliente Supabase:**

```typescript
// TODO: em mobile/src/lib/supabase.ts → createClient com AsyncStorage como storage
// TODO: exportar supabase como singleton
```

<details>
<summary>💡 Solução — Passo 2</summary>

```typescript
// mobile/src/lib/supabase.ts
import { createClient } from '@supabase/supabase-js';
import AsyncStorage from '@react-native-async-storage/async-storage';

export const supabase = createClient(
  process.env.EXPO_PUBLIC_SUPABASE_URL!,
  process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!,
  {
    auth: {
      storage: AsyncStorage,
      autoRefreshToken: true,
      persistSession: true,
      detectSessionInUrl: false,
    },
  }
);
```

</details>

---

> **Passo 3 — Criar cliente axios com interceptor de JWT:**

```typescript
// TODO: em mobile/src/lib/api.ts → criar instância axios com baseURL = EXPO_PUBLIC_API_URL
// TODO: interceptor de request: buscar sessão do supabase, adicionar Authorization: Bearer token
// TODO: exportar api como singleton
```

<details>
<summary>💡 Solução — Passo 3</summary>

```typescript
// mobile/src/lib/api.ts
import axios from 'axios';
import { supabase } from './supabase';

export const api = axios.create({
  baseURL: process.env.EXPO_PUBLIC_API_URL,
});

api.interceptors.request.use(async (config) => {
  const { data } = await supabase.auth.getSession();
  if (data.session?.access_token) {
    config.headers.Authorization = `Bearer ${data.session.access_token}`;
  }
  return config;
});
```

</details>

---

### Checkboxes

- [ ] **T1.1:** `npx expo start` roda sem erro após instalar dependências
- [ ] **T1.2:** `supabase.auth.getSession()` chamado no REPL retorna objeto válido (não lança erro)
- [ ] **T1.3:** `api.get('/health')` retorna `{"status": "ok"}` (confirma que API URL está correta)

---

### Resumo
> *Preencher após concluir a task.*
