# Task 1: Setup Streamlit + design system ✅

**Status:** Concluído (MVP Streamlit — React Native pós-POC)  
**Dev:** Frontend Dev 2

## O que foi feito

Projeto Streamlit em `frontend/app/` com design system via CSS injetado:

```
frontend/app/
  app.py      → entrypoint, st.navigation, estado global do session_state
  _utils.py   → inject_css(), bootstrap de sys.path
  pages/
    1_dashboard.py
    2_estoque.py
    3_chat.py
```

**Tokens de design (`_utils.py`):**
- Fonte: Space Grotesk + Space Mono (Google Fonts)
- Sidebar: `#ede9dc` com borda `#d6d1c4`
- Metric cards: `#f5f2ea`, border-radius `0.75rem`
- Alert badge: `#cb785c` (terracota)
- Botões: border-radius pill `9999px`

```bash
uv run streamlit run frontend/app/app.py
```

### Checkboxes

- [x] **T1.1:** Streamlit instalado e rodando
- [x] **T1.2:** CSS injetado — fonte e paleta aplicadas globalmente
- [x] **T1.3:** Sidebar com branding e navegação entre 3 páginas
