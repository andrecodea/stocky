# Task 1: Setup React Native + design tokens

**Estimativa:** ~30 min  
**Dev:** Frontend Dev 2  
**O que é e por que existe:**  
Criar o projeto React Native e definir tokens de design (cores, tipografia, espaçamentos) em um arquivo central. Tokens centralizados evitam valores hardcoded espalhados pelos componentes.

**Arquivos:**
- Criar: projeto `mobile/` via Expo
- Criar: `mobile/src/theme/index.ts`

---

### Documentação
- [Expo CLI](https://docs.expo.dev/more/expo-cli/) — `npx create-expo-app`
- [React Navigation](https://reactnavigation.org/docs/getting-started) — navegação para RN

---

### Decisões antes de codar

```
// TODO: definir paleta de cores (sugestão: verde para estoque ok, laranja para alerta, vermelho para crítico)
// TODO: definir se usar NativeWind (Tailwind para RN) ou StyleSheet nativo
// TODO: confirmar que emulador Android está disponível
```

---

### O que você precisa fazer

> **Passo 1 — Criar projeto Expo:**

```bash
# TODO: criar projeto → npx create-expo-app mobile --template blank-typescript
# TODO: entrar na pasta e testar → cd mobile && npx expo start
# TODO: abrir no emulador Android — verificar que tela default aparece
```

<details>
<summary>💡 Solução — Passo 1</summary>

```bash
npx create-expo-app mobile --template blank-typescript
cd mobile
npx expo start
# Pressionar 'a' para abrir no emulador Android
```

</details>

---

> **Passo 2 — Instalar React Navigation:**

```bash
# TODO: npx expo install @react-navigation/native @react-navigation/bottom-tabs
# TODO: npx expo install react-native-screens react-native-safe-area-context
```

<details>
<summary>💡 Solução — Passo 2</summary>

```bash
npx expo install @react-navigation/native @react-navigation/bottom-tabs
npx expo install react-native-screens react-native-safe-area-context
```

</details>

---

> **Passo 3 — Criar arquivo de tema:**

```typescript
// TODO: em mobile/src/theme/index.ts → exportar objeto theme com:
//   colors: { primary, background, surface, text, textMuted, success, warning, danger, border }
//   spacing: { xs: 4, sm: 8, md: 16, lg: 24, xl: 32 }
//   fontSize: { sm: 12, md: 14, lg: 16, xl: 20, xxl: 24 }
//   borderRadius: { sm: 4, md: 8, lg: 16 }
```

<details>
<summary>💡 Solução — Passo 3</summary>

```typescript
// mobile/src/theme/index.ts
export const theme = {
  colors: {
    primary: '#2D7D46',
    background: '#F5F5F5',
    surface: '#FFFFFF',
    text: '#1A1A1A',
    textMuted: '#6B7280',
    success: '#16A34A',
    warning: '#D97706',
    danger: '#DC2626',
    border: '#E5E7EB',
  },
  spacing: { xs: 4, sm: 8, md: 16, lg: 24, xl: 32 },
  fontSize: { sm: 12, md: 14, lg: 16, xl: 20, xxl: 24 },
  borderRadius: { sm: 4, md: 8, lg: 16 },
} as const;
```

</details>

---

### Checkboxes

- [ ] **T1.1:** `npx expo start` + emulador Android abre app sem crash
- [ ] **T1.2:** React Navigation instalado — sem erro de dependência
- [ ] **T1.3:** `theme.colors.primary` exportado e importável em qualquer tela

---

### Resumo
> *Preencher após concluir a task.*
