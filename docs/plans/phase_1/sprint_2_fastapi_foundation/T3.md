# Task 3: Middleware de Auth + endpoint de login

**Estimativa:** ~40 min  
**Dev:** Backend Dev 1  
**O que é e por que existe:**  
Implementar autenticação via Supabase Auth — login retorna JWT, e um `Depends()` do FastAPI verifica o JWT em todos os endpoints protegidos. Sem isso, nenhum endpoint dos próximos sprints consegue saber quem é o usuário.

**Arquivos:**
- Editar: `app/routers/auth.py`
- Criar: `app/dependencies.py` (função `get_current_user`)
- Editar: `app/main.py` (registrar router)

---

### Conceito: FastAPI `Depends()` para autenticação

> `Depends(get_current_user)` é injetado nos parâmetros de qualquer endpoint.  
> FastAPI chama `get_current_user` automaticamente antes do handler, e injeta o usuário ou lança `401`.  
> Sem isso, cada endpoint precisa verificar o token manualmente — duplicação e possibilidade de esquecer.

---

### Documentação
- [`supabase.auth.sign_in_with_password()`](https://supabase.com/docs/reference/python/auth-signinwithpassword) — login com email/senha
- [`supabase.auth.get_user(jwt)`](https://supabase.com/docs/reference/python/auth-getuser) — valida JWT e retorna usuário
- [FastAPI Dependencies](https://fastapi.tiangolo.com/tutorial/dependencies/) — `Depends()` para injeção

---

### O que você precisa fazer

> **Passo 1 — Criar modelo Pydantic para o usuário atual:**
>
> **Você tem disponível:** `app/models/usuario.py` vazio; tabela `perfis` com campos `id`, `nome`, `role`

```python
# TODO: em app/models/usuario.py → criar UsuarioAtual(BaseModel) com:
#   id: str
#   email: str
#   nome: str
#   role: str  (literalmente "operador" ou "admin")
```

<details>
<summary>💡 Solução — Passo 1</summary>

```python
# app/models/usuario.py
from pydantic import BaseModel
from typing import Literal

class UsuarioAtual(BaseModel):
    id: str
    email: str
    nome: str
    role: Literal["operador", "admin"]
```

</details>

---

> **Passo 2 — Criar `get_current_user` como dependency:**
>
> **Você tem disponível:** `supabase_client` de `app.database`; `UsuarioAtual` de `app.models.usuario`

```python
# TODO: em app/dependencies.py → criar função get_current_user(token: str = Depends(oauth2_scheme))
# TODO: chamar supabase_client.auth.get_user(token) para validar JWT
# TODO: se inválido → raise HTTPException(status_code=401, detail="Token inválido")
# TODO: buscar perfil do usuário na tabela perfis via supabase_client
# TODO: retornar UsuarioAtual com dados combinados (auth + perfil)
```

<details>
<summary>💡 Solução — Passo 2</summary>

```python
# app/dependencies.py
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer
from app.database import supabase_client
from app.models.usuario import UsuarioAtual

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/auth/login")

async def get_current_user(token: str = Depends(oauth2_scheme)) -> UsuarioAtual:
    try:
        user_response = supabase_client.auth.get_user(token)
        user = user_response.user
    except Exception:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Token inválido")

    perfil = (
        supabase_client.table("perfis")
        .select("nome, role")
        .eq("id", user.id)
        .single()
        .execute()
    )

    return UsuarioAtual(
        id=user.id,
        email=user.email,
        nome=perfil.data["nome"],
        role=perfil.data["role"],
    )
```

</details>

---

> **Passo 3 — Criar router de auth com endpoint de login:**
>
> **Você tem disponível:** `app/routers/auth.py` vazio

```python
# TODO: em app/routers/auth.py → criar APIRouter com prefix="/auth"
# TODO: endpoint POST /login aceita OAuth2PasswordRequestForm (username=email, password)
# TODO: chamar supabase_client.auth.sign_in_with_password({"email": ..., "password": ...})
# TODO: retornar {"access_token": session.access_token, "token_type": "bearer"}
# TODO: se credenciais inválidas → HTTPException 401
```

<details>
<summary>💡 Solução — Passo 3</summary>

```python
# app/routers/auth.py
from fastapi import APIRouter, Depends, HTTPException, status
from fastapi.security import OAuth2PasswordRequestForm
from app.database import supabase_client

router = APIRouter(prefix="/auth", tags=["auth"])

@router.post("/login")
async def login(form: OAuth2PasswordRequestForm = Depends()):
    try:
        response = supabase_client.auth.sign_in_with_password({
            "email": form.username,
            "password": form.password,
        })
        return {
            "access_token": response.session.access_token,
            "token_type": "bearer",
        }
    except Exception:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Email ou senha inválidos",
        )
```

</details>

---

> **Passo 4 — Registrar router e testar endpoint protegido:**
>
> **Você tem disponível:** `app/main.py` com `/health`; router de auth criado

```python
# TODO: em app/main.py → incluir router de auth: app.include_router(auth.router)
# TODO: criar endpoint GET /me protegido: depende de get_current_user, retorna o usuário atual
# TODO: testar no Swagger UI: login → copiar token → usar no /me → deve retornar dados do usuário
```

<details>
<summary>💡 Solução — Passo 4</summary>

```python
# app/main.py (adicionar ao existente)
from app.routers import auth
from app.dependencies import get_current_user
from app.models.usuario import UsuarioAtual

app.include_router(auth.router)

@app.get("/me")
async def me(usuario: UsuarioAtual = Depends(get_current_user)):
    return usuario
```

</details>

---

### Checkboxes

- [ ] **T3.1:** `POST /auth/login` com credenciais válidas retorna `access_token`
- [ ] **T3.2:** `GET /me` sem token retorna `401 Unauthorized`
- [ ] **T3.3:** `GET /me` com token válido retorna `{"id": "...", "email": "...", "nome": "...", "role": "operador"}`

---

### Resumo
> *Preencher após concluir a task.*
