# Task 3: Auth middleware + endpoint de login 🔄

**Status:** Em progresso  
**Dev:** Backend Dev 1  
**Estimativa:** ~40 min  
**Arquivos:**
- Criar: `backend/api/dependencies.py`
- Editar: `backend/api/routers.py` (adicionar router de auth)
- Editar: `backend/api/app.py` (registrar router)

---

### O que você precisa fazer

> **Passo 1 — Criar `get_current_user` como dependency:**
>
> **Você tem disponível:** `backend/db/supabase.py` com `get_client()`

```python
# TODO: criar backend/api/dependencies.py
# TODO: função get_current_user(token: str = Depends(oauth2_scheme))
# TODO: chamar get_client().auth.get_user(token) para validar JWT
# TODO: se inválido → HTTPException 401
# TODO: buscar perfil na tabela perfis via get_client()
# TODO: retornar dict com id, email, nome, role
```

<details>
<summary>💡 Solução — Passo 1</summary>

```python
# backend/api/dependencies.py
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer
from backend.db.supabase import get_client

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/auth/login")

async def get_current_user(token: str = Depends(oauth2_scheme)) -> dict:
    try:
        user = get_client().auth.get_user(token).user
    except Exception:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Token inválido")

    perfil = (
        get_client().table("perfis")
        .select("nome, role")
        .eq("id", user.id)
        .single()
        .execute()
    )
    return {"id": user.id, "email": user.email, **perfil.data}
```

</details>

---

> **Passo 2 — Criar router de auth com `POST /auth/login`:**
>
> **Você tem disponível:** `backend/api/routers.py` (vazio)

```python
# TODO: em backend/api/routers.py → criar APIRouter com prefix="/auth"
# TODO: POST /login aceita OAuth2PasswordRequestForm
# TODO: chamar get_client().auth.sign_in_with_password({"email": ..., "password": ...})
# TODO: retornar {"access_token": session.access_token, "token_type": "bearer"}
```

<details>
<summary>💡 Solução — Passo 2</summary>

```python
# backend/api/routers.py
from fastapi import APIRouter, Depends, HTTPException, status
from fastapi.security import OAuth2PasswordRequestForm
from backend.db.supabase import get_client

auth_router = APIRouter(prefix="/auth", tags=["auth"])

@auth_router.post("/login")
async def login(form: OAuth2PasswordRequestForm = Depends()):
    try:
        response = get_client().auth.sign_in_with_password({
            "email": form.username,
            "password": form.password,
        })
        return {"access_token": response.session.access_token, "token_type": "bearer"}
    except Exception:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Credenciais inválidas")
```

</details>

---

> **Passo 3 — Registrar router e endpoint `/me`:**
>
> **Você tem disponível:** `backend/api/app.py` com a instância `app`

```python
# TODO: em backend/api/app.py → app.include_router(auth_router)
# TODO: endpoint GET /me → Depends(get_current_user), retorna o usuário atual
```

<details>
<summary>💡 Solução — Passo 3</summary>

```python
# backend/api/app.py (adicionar)
from backend.api.routers import auth_router
from backend.api.dependencies import get_current_user

app.include_router(auth_router)

@app.get("/me")
async def me(usuario: dict = Depends(get_current_user)):
    return usuario
```

</details>

---

### Checkboxes

- [ ] **T3.1:** `POST /auth/login` com credenciais válidas retorna `access_token`
- [ ] **T3.2:** `GET /me` sem token retorna `401 Unauthorized`
- [ ] **T3.3:** `GET /me` com token válido retorna `{"id": "...", "email": "...", "role": "operador"}`
