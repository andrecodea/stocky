# Task 2: Settings + cliente Supabase ✅

**Status:** Concluído  
**Dev:** Backend Dev 1

## O que foi feito

Ambos já estão implementados:

**`backend/config.py`** — Settings com pydantic-settings, lê `.env` automaticamente:
```python
class Settings(BaseSettings):
    supabase_url: str
    supabase_anon_key: str
    anthropic_api_key: str = ""
    openrouter_api_key: str = ""
    # ... outros campos
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")
```

**`backend/db/supabase.py`** — singleton lazy do cliente Supabase:
```python
def get_client() -> Client:
    global _client
    if _client is None:
        _client = create_client(settings.supabase_url, settings.supabase_anon_key)
    return _client
```

### Checkboxes

- [x] **T2.1:** `settings.supabase_url` carrega corretamente do `.env`
- [x] **T2.2:** `get_client()` conecta ao Supabase sem erro
- [x] **T2.3:** App falha com `ValidationError` se `.env` não tiver `SUPABASE_URL`
