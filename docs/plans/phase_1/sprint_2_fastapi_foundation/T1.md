# Task 1: Estrutura FastAPI ✅

**Status:** Concluído  
**Dev:** Backend Dev 1

## O que foi feito

A estrutura base está em `backend/api/`:

```
backend/
  api/
    app.py       → instância FastAPI, CORS, /ping, /health
    routers.py   → routers (preencher nas próximas tasks)
  config.py      → Settings com pydantic-settings
  db/
    supabase.py  → cliente Supabase singleton
  services/      → lógica de negócio desacoplada da API
```

Para rodar:
```bash
uv run python backend/api/app.py
# ou: uv run uvicorn backend.api.app:app --reload --port 8500
```

### Checkboxes

- [x] **T1.1:** FastAPI instalado (`uv sync`)
- [x] **T1.2:** Estrutura de pastas criada em `backend/`
- [x] **T1.3:** `GET /health` retorna `{"status": "ok"}` — Swagger em `/docs`
